# ProblemBridge AI Environment Configuration
PORT=3000
NODE_ENV=development
JWT_SECRET=problembridge_super_secret_jwt_key_2026_change_in_production

# Optional MongoDB Connection String (leave blank to use built-in zero-config database)
# MONGODB_URI=mongodb://localhost:27017/problembridge

# Optional Google Gemini API Key (for external LLM generation; leaves intelligent local NLP engine active if blank)
# GEMINI_API_KEY=your_gemini_api_key_here
